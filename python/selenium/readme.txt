1、pip3安装selenium
macos下，已存在python3环境。(即存在pip3库管理工具)
pip3 Install selenuim
pip3 show selenium可查看安装的位置(存在于python安装包/lib/site-packages);若是在python项目中,似乎会存在于该项目独立的site-packages ?

2、homebrew安装chromedriver

3、在macos中需要将chrome放在应用程序(启动台)中。不然chromedriver可能会找不到chrome的二进制文件。