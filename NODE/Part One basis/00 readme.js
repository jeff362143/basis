/**
 * commonJs规范：
 * require引入和module.exports导出（文件的引入导出都是以变量形式）
 * 该规范是用来引入js文件的，因此导入时可省略.js后缀名
 * 
 * http模块createServer方法创建服务器
 * fs模块的appendFile方法、unlink方法创建和删除文件，rename修改文件名（回调函数的第一个参数都是error）
 * fs模块的mkdir，rmdir，readdir方法可以用于创建，删除，读取文件夹信息（readdirSync异步读取）
 * fs模块的stat方法可以用来分辨文件和文件夹
 * fs模块的readFileSync方法用于获取文件的butter流
 */

