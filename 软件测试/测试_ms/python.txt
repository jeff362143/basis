1、字典、元祖、列表的区别？
元祖和列表是可迭代对象，字典不是；
列表是可变类型，元祖是不可变类型，字典的键值是可变的，键名不可变。

2、列表、字典、元祖常见方法？
列表insert、append、pop
元祖的话 count()、index()、__len__() 	__len__()这个方法列表、字典、元祖都有的吧
字典的话删除是pop，新增的话可以直接字典['key']新增。

3、python常用模块
time模块, random模块，re模块、os模块

