 /**
 * DOM
 * 
 * document对象就是 html文档
 * document.body.style.color和getAttribute()
 * getAttribute是用于获取标签属性的，不能用于获取css样式
 * style方法只能获取元素的内联元素（普通css样式不能获取），也只能设置元素的内联元素
 * 
 * 
 * mouse事件：
 * onclick事件
 * ondbclick事件
 * onmouseenter事件
 * onmouseleave事件
 * onmousedown事件
 * onmouseup事件
 * onfocus事件
 * onblur事件 
 * 
 * 绝对定位初始位置是根据文档流确定的（相对它原本的位置）
 * 
 * document.getElementById("btn").value = "xxx"不使用.style也可以直接修改元素的属性
 * 
 * document.getElementsByTagName获取类数组对象(按首次出现位置对应)
 * 
 */

