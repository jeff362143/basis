该项目为spring-boot框架一个最基本的demo,仅实现了设计api接口返回json数据.
实现这样一个demo,需要的最小配置：
1、需要下载maven管理工具以及基于maven下载的spring-boot依赖(pom.xml)；且需要在idea中配置maven和maven下载依赖的存放位置
2、userControl类中利用springboot注解返回一个json数据代码，main方法中调用userControl
3、edit configue中创建一个Application，选择springboot并将main方法路径配置上。



